# Frontend Environment Variables
# Copy this file to .env.local and update with your values

# Backend API URL
# This is the URL where your backend server is running
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# GraphQL API URL (optional, defaults to Rick and Morty API)
# NEXT_PUBLIC_GRAPHQL_URL=https://rickandmortyapi.com/graphql

# Note: In Next.js, only environment variables prefixed with NEXT_PUBLIC_ 
# are exposed to the browser. Other variables are only available server-side.
